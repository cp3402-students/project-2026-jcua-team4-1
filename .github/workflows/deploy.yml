name: Deploy WordPress Theme to Lightsail

on:
  push:
    branches:
      - main
      - staging

jobs:
  deploy-staging:
    if: github.ref == 'refs/heads/staging'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build theme
        run: |
          cd wp-content/themes/your-theme
          npm install
          npm run build

      - name: Package theme
        run: tar -czf theme.tar.gz wp-content/themes/kc-blast-theme

      - name: Upload to staging server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.STAGING_HOST }}
          username: ${{ secrets.STAGING_USER }}
          key: ${{ secrets.STAGING_SSH_KEY }}
          source: "theme.tar.gz"
          target: "/home/deploy/"

      - name: Deploy on staging
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.STAGING_HOST }}
          username: ${{ secrets.STAGING_USER }}
          key: ${{ secrets.STAGING_SSH_KEY }}
          script: |
            cd /home/deploy
            tar -xzf theme.tar.gz
            sudo rsync -avz wp-content/themes/kc-blast-theme /opt/bitnami/wordpress/wp-content/themes/kc-blast-theme
            rm theme.tar.gz

  deploy-production:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build theme
        run: |
          cd wp-content/themes/your-theme
          npm install
          npm run build

      - name: Package theme
        run: tar -czf theme.tar.gz wp-content/themes/your-theme

      - name: Upload to production server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.PROD_HOST }}
          username: ${{ secrets.PROD_USER }}
          key: ${{ secrets.PROD_SSH_KEY }}
          source: "theme.tar.gz"
          target: "/home/deploy/"

      - name: Deploy on production
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.PROD_HOST }}
          username: ${{ secrets.PROD_USER }}
          key: ${{ secrets.PROD_SSH_KEY }}
          script: |
            cd /home/deploy
            tar -xzf theme.tar.gz
            sudo rsync -avz wp-content/themes/kc-blast-theme /opt/bitnami/wordpress/wp-content/themes/kc-blast-theme
            rm theme.tar.gz
